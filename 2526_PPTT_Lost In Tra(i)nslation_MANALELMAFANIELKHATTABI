
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML & CSS</title>

    <!-- Puedes borrar estos si vas a usar solo el <style> de abajo -->
    <!-- <link rel="stylesheet" href="assets/styles.css">
    <link rel="stylesheet" href="assets/flexboxes.css">
    <link rel="stylesheet" href="assets/nav.css">
    <link rel="stylesheet" href="assets/typography.css"> -->

    <style>
        /* --------- ESTILOS GENERALES --------- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            min-height: 100vh;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(120deg, #c4efcb, #abd9bd, #0f766e);
            background-size: 200% 200%;
            animation: bg-move 18s ease-in-out infinite alternate;
            color: #111827;
        }

        @keyframes bg-move {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        .container {
            width: min(1000px, 95vw);
            background: #f9fafb;
            border-radius: 18px;
            box-shadow: 0 18px 45px rgba(0, 0, 0, 0.35);
            padding: 24px 28px 28px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            animation: fade-in-up 0.7s ease-out both;
        }

        @keyframes fade-in-up {
            from {
                opacity: 0;
                transform: translateY(25px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* --------- CABECERA / TÍTULO --------- */
        .title {
            text-align: center;
            font-weight: 700;
            font-size: clamp(1.4rem, 2vw + 0.8rem, 1.9rem);
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #0f172a;
            position: relative;
            padding-bottom: 8px;
            animation: title-glow 2.4s ease-in-out infinite alternate;
        }

        .title::after {
            content: "";
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            bottom: 0;
            width: 60px;
            height: 3px;
            border-radius: 999px;
            background: linear-gradient(90deg, #0f766e, #22c55e);
        }

        @keyframes title-glow {
            0% {
                text-shadow: 0 0 0 rgba(34, 197, 94, 0.0);
            }
            100% {
                text-shadow: 0 0 14px rgba(34, 197, 94, 0.6);
            }
        }

        /* --------- LAYOUT PRINCIPAL --------- */
        .content-layout {
            display: grid;
            grid-template-columns: minmax(160px, 180px) 1fr;
            gap: 26px;
        }

        @media (max-width: 720px) {
            .content-layout {
                grid-template-columns: 1fr;
            }
        }

        /* --------- MENÚ LATERAL --------- */
        nav {
            align-self: flex-start;
        }

        .vertical-buttons {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 10px;
            padding: 0;
            margin: 0;
        }

        .vertical-buttons li {
            opacity: 0;
            transform: translateX(-18px);
            animation: slide-in-left 0.6s ease-out forwards;
        }

        /* Retraso escalonado para cada botón */
        .vertical-buttons li:nth-child(1) { animation-delay: 0.15s; }
        .vertical-buttons li:nth-child(2) { animation-delay: 0.25s; }
        .vertical-buttons li:nth-child(3) { animation-delay: 0.35s; }
        .vertical-buttons li:nth-child(4) { animation-delay: 0.45s; }
        .vertical-buttons li:nth-child(5) { animation-delay: 0.55s; }
        .vertical-buttons li:nth-child(6) { animation-delay: 0.65s; }

        @keyframes slide-in-left {
            from {
                opacity: 0;
                transform: translateX(-18px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .vertical-buttons a {
            display: block;
            padding: 10px 14px;
            border-radius: 999px;
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            color: #0f172a;
            background: #e5e7eb;
            border: 1px solid transparent;
            transition:
                transform 0.2s ease,
                box-shadow 0.2s ease,
                background 0.2s ease,
                border-color 0.2s ease;
        }

        .vertical-buttons a:hover {
            transform: translateY(-2px) scale(1.02);
            background: #9ad8c1;
            color: #70e3a0;
            border-color: #0f766e;
            box-shadow: 0 10px 18px rgba(32, 106, 42, 0.35);
        }

        .vertical-buttons a:active {
            transform: translateY(0) scale(0.99);
            box-shadow: none;
        }

        /* --------- CONTENIDO PRINCIPAL --------- */
        .main-content {
            background: rgb(179, 214, 193);
            border-radius: 16px;
            padding: 18px 20px 20px;
            box-shadow: 0 10px 28px rgba(60, 84, 67, 0.12);
            border: 1px solid #aad8a5;
            animation: fade-in 0.8s ease-out 0.3s both;
        }

        @keyframes fade-in {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .main-content h1 {
            font-size: 1.6rem;
            margin-bottom: 4px;
            color: #0f172a;
        }

        .main-content h2 {
            font-size: 1.1rem;
            margin-bottom: 14px;
            color: #448056;
            font-weight: 500;
        }

        .main-content ul {
            margin-left: 18px;
            display: flex;
            flex-direction: column;
            gap: 6px;
            margin-top: 8px;
        }

        .main-content li {
            position: relative;
            padding-left: 4px;
            list-style: disc;
            font-size: 0.98rem;
            color: #0a1c15;
            opacity: 0;
            transform: translateY(8px);
            animation: list-pop 0.5s ease-out forwards;
        }

        .main-content li:nth-child(1) { animation-delay: 0.45s; }
        .main-content li:nth-child(2) { animation-delay: 0.6s; }
        .main-content li:nth-child(3) { animation-delay: 0.75s; }

        @keyframes list-pop {
            from {
                opacity: 0;
                transform: translateY(8px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="title">
            Manal El Madani El Khattabi
        </div>

        <div class="content-layout">
            <nav>
                <ul class="vertical-buttons">
                    <li><a href="week1.html">Week1</a></li>
                    <li><a href="week2.html">Week2</a></li>
                    <li><a href="week3.html">Week3</a></li>
                    <li><a href="week4.html">Week4</a></li>
                    <li><a href="week5.html">Week5</a></li> 
                    <li><a href="week6.html">Week6</a></li> 
                    <li><a href="week7.html">Week7</a></li> 
                    <li><a href="week8.html">Week8</a></li> 
                    <li><a href="week9.html">Week9</a></li>    
                    <li><a href="week10.html">Week10</a></li> 
                    <li><a href="contact.html">Contact</a></li>
                </ul>
            </nav>

            <main class="main-content">
                <h1>Lost In Tra(i)nslation</h1>
             
</body>
         <img src="trein.png" alt="(schets)" width="700" height="500">

</html>
